# Turtlebot2 root file system files

In this directory we save files that we installed on the TB2 under the root (/) file system.
I.e., udev scripts

Register the init.d scripts with:

	sudo update-rc.d rplidar.sh defaults
